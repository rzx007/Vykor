# 模型网络重试实施交接 Prompt

以下代码块可完整复制给执行智能体。执行规范以当前仓库 AGENTS.md 和关联设计、计划为准；本文件本身不是实施完成记录。

```text
请在 D:\code\personal-project\OpenHarness-ts 实施模型网络自动重试，完成代码、定向测试及文档更新，不要仅提交方案。用户已授权按下述计划实施；无需为普通、可逆的实现步骤重复请求确认。不要自动发布、推送、合并或执行破坏性操作。

先完整阅读：
1. 当前工作区适用的 AGENTS.md。
2. docs/model-network-retry-design.md
3. docs/superpowers/plans/2026-09-25-model-network-retry.md

执行顺序：任务 0 → 1 → 2 → 3 → 3A → 4 → 5 → 5A → 6 → 7 → 8。特别注意 3A 和 5A 是审核后补入的必做任务。使用适用的执行计划、调试和验证技能；只加载当前任务需要的技能。需要时可使用子代理，但共享接口和依赖任务必须串行，主智能体负责集成和验收。

开始前：
- 记录 git status、HEAD、既有测试失败。计划二次审核基于 45891233，比较其后相关变更，不假设代码未变化。
- 用户已有修改必须保留；不要 git reset、整文件覆盖或顺手重构。发现过期路径时定位实际入口，并同步更新计划。
- 按任务写有意义的失败测试再实现，完成后更新任务勾选和实际验证记录。不要把文档自检勾选当成实现完成。

核心目标：
短暂网络故障后自动、有界、可取消地重试当前模型调用，显示等待过程，最终回答不重复，已经完成的工具不重复执行。不要把整个 Run、submitMessage、权限请求或工具执行包进重试循环。

必须守住的边界与易错点：
1. 重试次数只有一个负责人：主生成由 QueryEngine 控制；适配器单次请求，SDK maxRetries=0。request/stream 分类及总预算都有效。恢复截止时间不会因为收到部分文字而不断延长。用户取消优先于超时，必须中止底层连接和等待。
2. 压缩和记忆提取也使用原始适配器，关闭旧重试后必须接入任务 3A 的独立有界恢复。辅助调用先缓冲，完整成功后才交付；失败前缀不得拼进摘要/JSON，记忆写入在重试循环外。不要将辅助包装器套到主生成造成双层重试。
3. 同一生成重试固定使用同一份已确认输入、模型客户端、工具定义、reasoningEffort 和 maxOutputTokens/API maxTokens；重试不再次压缩、不消费 steer。保留新增 setOutputReserve 行为，下一正常回合再应用配置变化。
4. length/max_tokens 是长度受限结束，不是网络断流。正文 → 必要的截断提示 → 最终 complete/提交。提示只能出现一次，complete 后不能再创建同次生成的未提交提示。Codex response.incomplete 按原因区分；不执行截断的工具参数，也不把坏 JSON 变成 {} 执行。
5. 每次尝试有 generationId/attempt。失败输出替换必须同时覆盖引擎 history、FrameworkAgentRun.output、持久 part、客户端展示。上一成功生成的文字和工具结果不能被清空。停止旧 iterator，禁止迟到数据进入新尝试。
6. 客户端隐藏还不够：任务 5A 必须过滤 buildAgentTranscript 的文字、思考、reasoningReplay 和工具配对，并检查公开文本、导出、rewind 消费者。superseded 或新格式未 committed 内容不能作为完成历史送回模型。旧格式保留兼容语义，诊断原始内容不得物理删除。
7. 用量按尝试结算一次。累计 usage 快照不能相加；未知用量不填 0。已知消耗保留，不完整标记经事件、Run metadata、snapshot、最终结果传到界面。model.attempt.finished 和 usage.updated 不能各计一次费用；重复事件不增加未知次数。
8. Desktop 已按约 50ms 合并会话快照，renderer 不一定收到每个中间事件。最新快照必须足以处理旧 part 失效、等待清理和用量完整性。不要为实现重试恢复逐 token 全量 IPC。必须跑订阅 coalescing 集成测试，不只测试提示组件。
9. 重试期间 Run 保持运行中，只有耗尽/不可重试错误才失败。现有卡住检测不能提前结束合法等待，也不能因重试 metadata 获得无限豁免。终态、取消和切换会话后迟到事件不得复活旧任务。
10. apps/frontend 是 React/OpenTUI 终端界面，不是 Web；用 Bun 测试、原生 text/box。Desktop 用现有 React 组件。CLI 已打印字符不可撤回，明确标注旧输出中断；最终机器结果和流式替换语义必须正确，不能声称已抹掉终端历史。

实现范围与验证：
- 不新增重试依赖、数据库表或第二套客户端重连；复用现有事件、metadata、事务和投影失败处理。数据库/事件交付失败不是模型网络错误，不能因此重发模型。
- 保留现有工具失败恢复、安全路径限制及输出上限功能；网络重试不消耗工具回合或触发重复工具。
- 严格按计划运行定向测试，新增测试验证实际请求次数、工具次数、事件顺序、数据库重载后的模型输入、快照恢复、未知用量与取消。使用 mock/可控时钟，不请求真实模型或靠真实断网验证。
- 必做组合用例：网络失败后成功；网络失败后长度受限；先完成工具再断网；重试后重载 Agent 再提问；辅助调用失败后成功且只写一次；未知消耗后已知成功；Desktop 同一合并窗口内失效到成功；重试期间取消/重连。
- 协议版本按实施时当前值增加，并更新对应握手及兼容检查。任务 2～7（包含 3A、5A）作为一组交付，不能在消费者未接好时宣称可发布。
- 测试命令见计划；环境缺少 Bun 或检查受阻时如实记录未运行及原因，不把类型检查代替行为测试。优先解决本次引入失败，基线问题单列，避免反复全仓测试。

完成交付：
报告实际修改、完成的任务、通过的检查、未通过/未运行项目及原因、剩余风险和协议变化。只有相应验收通过后才将文档标为已实现。若真实代码与计划冲突，先核实并作最小必要调整；只有缺少关键决定或需要扩大授权时才暂停提问，不自行删减上述验收要求。
```
