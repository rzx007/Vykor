# 项目协作与技能使用

- 文档、代码说明和面向用户的回复使用通俗、具体的语言；协作约定见同目录的 AGENTS.md。
- 简单、明确、可逆的修改直接执行，不额外要求设计文档、计划或审批。
- 架构设计、跨模块改造或需求明显不明确时，再使用合适的规划技能。
- 用户明确点名的技能应使用；其他技能只有在其具体知识或流程有助于当前任务时才加载，不按“可能沾边”触发。
- 技能中的通用流程建议不能覆盖用户明确要求，也不能自行增加审批或强制委派步骤；权限和安全边界仍须遵守。
- 对可自动验证的行为变化和 bug 修复，先补能复现问题的测试；纯文案和低风险配置调整不要求为了流程而新增测试。
- 测试和检查按风险及改动范围执行，避免重复运行完整测试套件；完成时说明实际验证结果。
- 只有涉及高风险、不可逆操作或缺少关键决策时才等待用户确认。局部受阻时继续完成不受影响的部分。

项目技能位于 `.agents/skills/`。使用运行时提供的技能目录发现并加载相关内容，不在本文件重复维护整份技能清单。
