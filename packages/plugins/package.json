{
  "name": "@vykor/plugins",
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    },
    "./sdk": {
      "types": "./src/sdk.ts"
    }
  },
  "scripts": {
    "build": "exit 0",
    "check-types": "tsc --noEmit && tsc -p tsconfig.sdk-tests.json && tsc -p tsconfig.sdk-examples.json",
    "test": "vitest run",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@vykor/core": "workspace:*",
    "zod": "catalog:",
    "@vykor/skills": "workspace:*",
    "@vykor/coordinator": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "^5.7.3",
    "vitest": "catalog:"
  }
}
